import * as Promise from 'bluebird';
import { Callback, Provider, Transaction } from '../../types';
export declare const validateBundle: (bundle: ReadonlyArray<Transaction>) => boolean;
/**
 * @method createGetBundle
 *
 * @memberof module:core
 *
 * @param {Provider} provider - Network provider for accessing IRI
 *
 * @return {function} {@link #module_core.getBundle `getBundle`}
 */
export declare const createGetBundle: (provider: Provider) => (tailTransactionHash: string, callback?: Callback<ReadonlyArray<Transaction>> | undefined) => Promise<ReadonlyArray<Transaction>>;
